import { Node } from "@/types/main.type"

export const metadata = {
  title: "Mohsen's Portfolio | Skills",
  description: "These are some skills I have.",
}

const Layout = ({ children }: Node) => children

export default Layout
