import { Node } from "@/types/main.type"

export const metadata = {
  title: "Mohsen's Portfolio | Projects",
  description: "These are the projects that I've done.",
}

const Layout = ({ children }: Node) => children

export default Layout
